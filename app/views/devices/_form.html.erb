<%= form_for [@device.venue, @device], url: venue_devices_path do |f| %>
  <% if @device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device.errors.count, "error") %> prohibited this device from being saved:</h2>

      <ul>
      <% @device.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :venue_id, :value => params[:venue_id]%>
  </div>
  <div class="field">
    <%= f.label "Nombre" %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label "Personas que entraron" %><br>
    <%= f.text_field :enter %>
  </div>
  <div class="field">
    <%= f.label "Personas que salieron" %><br>
    <%= f.text_field :leave %>
  </div>
  <div class="actions">
    <%= f.submit "Crear" %>
  </div>
<% end %>
